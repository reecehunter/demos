import { Typography } from "@mui/material"

const NotFoundPage = () => {
  return (
    <Typography component='h1' variant="h3">
        Page not found.
    </Typography>
  )
}

export default NotFoundPage